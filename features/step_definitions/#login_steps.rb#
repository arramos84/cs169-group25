Given /^that there is a user with the following email: (.*)$/ do |email|
  assert User.find(:all, :conditions => {:email => #{email}}) != nil
                     
  
                     
              


